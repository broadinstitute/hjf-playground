steps:
- name: 'alpine'
  id:  test-msg
  args: [ "env" ]
- name: 'broadinstitute/dsde-toolbox'
  id:  process-msg
  entrypoint: "/bin/bash"
  args:
    - '-eEuo'
    - 'pipefail'
    - '-c'
    - |-
      touch /workspace/rpms.txt
      env
      # echo "${_MSG}" > /workspace/msg.json 
      cat <<< "${_MSG}" > /workspace/msg.json
      cat /workspace/msg.json
      # jq -n --argjson data "${_MSG}" '.'
      #jq '.' <<< "${_MSG}"
      # jq . /workspace/msg.json
      # echo "${_MSG}" | jq .
      # echo "${_MSG}" | jq  -r .rpms[] > /workspace/rpms.txt
- name: 'gcr.io/google.com/cloudsdktool/google-cloud-cli'
  id: verify
  entrypoint: "/bin/bash"
  args:
    - '-eEuo'
    - 'pipefail'
    - '-c'
    - |-
      cat /workspace/rpms.txt
      cat /workspace/msg.json
